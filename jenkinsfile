pipeline {
    agent { docker {
            image 'python:3' } 
        }
    stages {
        stage('deploy') {
            steps {
                sh 'docker version'
                sh 'cp -r ./rp-portfolio /deploy'
            }
        }
    }
}
