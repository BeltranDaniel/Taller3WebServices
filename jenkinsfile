pipeline {
    agent any
    stages {
        stage('deploy') {
            steps {
                sh 'docker version'
                sh 'cp -r ./rp-portfolio /deploy'
            }
        }
    }
}
