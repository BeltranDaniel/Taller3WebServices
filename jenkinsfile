pipeline {
    agent any
    stages {
        stage('deploy') {
            steps {
                sh 'python --version'
                sh 'cp -r ./rp-portfolio /deploy'
            }
        }
    }
}
