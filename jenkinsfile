pipeline {
    agent any
    stages {
        stage('linter'){
            steps {
                sh 'pylint ./rp-portfolio'
            }
        }
        stage('deploy') {
            steps {
                sh 'cp -r ./rp-portfolio /deploy'
            }
        }
    }
}
